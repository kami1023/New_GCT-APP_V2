# Palette's Journal

## 2025-05-14 - Dashboard Search UX Enhancement
**Learning:** Imperative DOM manipulation for filtering in React apps leads to brittle UI and makes it harder to implement features like "No results found" or "Clear search". Using declarative state makes the search logic more predictable and easier to extend with accessibility features.
**Action:** Always prefer declarative state-driven filtering over direct DOM manipulation.
