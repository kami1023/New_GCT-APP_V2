## 2025-05-15 - Declarative Search UX
**Learning:** Replacing imperative DOM search logic with declarative React state filtering ensures predictable rendering and enables clean integration of empty state UI (e.g., 'No results found').
**Action:** Always prefer state-driven filtering for search/list components to maintain React's single source of truth and simplify empty state handling.
