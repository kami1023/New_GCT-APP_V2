## 2026-04-11 - Declarative Search UX
**Learning:** Replacing imperative DOM-based search/filtering with declarative React state ensures a more reliable UI, improves accessibility (e.g., proper ARIA labels), and enables the easy implementation of empty state feedback.
**Action:** Always prefer React state for filtering logic; use it to provide clear visual feedback when no results are found.
