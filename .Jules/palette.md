## 2025-05-14 - Declarative Search vs Imperative DOM
**Learning:** Replacing imperative DOM manipulation (`querySelectorAll`) with declarative React state filtering ensures predictable rendering and enables clean integration of empty state UI (e.g., 'No results found').
**Action:** Always prefer React state-driven filtering for search interfaces to maintain component synchronization and simplify the implementation of empty states and clear-search actions.
